# The test harness is development-only. It ships nothing the extension needs at run time, and it
# reaches out to ../../rules/doorman-tiers.md, which does not exist inside an installed extension.
hook/test-hook.mjs

# Module docs. They describe the repo layout, which does not exist inside an installed extension.
AGENTS.md
CLAUDE.md

# Sources and build config. `out/` is what runs.
src/**
tsconfig.json
node_modules/**
*.vsix
